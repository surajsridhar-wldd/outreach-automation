{
  "name": "outreach-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mongo-driven nudge worker. Runs in GitHub Actions, never on Vercel.",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "mongodb": "^6.9.0"
  }
}
